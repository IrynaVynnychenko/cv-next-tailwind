# Archive

One-time content-migration scripts (and the translation data files they consumed) used to add
`de`/`fr`/`es`/`it`/`tr` localized strings to `src/data/blog/*.ts`. All of them have already run
successfully and every target string now has every locale in the data files, so they are dead
code going forward. Kept here for history/reference only - do not wire them back into `package.json`.
